# Rejected Candidates

Candidate claims that were considered and rejected, with explicit reasons. Kept for audit so the
same rejected idea is not re-staged. Format: `## Claim` then `**Why rejected:** reason`.
