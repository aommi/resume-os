Daily pipeline brief. You are an agent running headless via the CLI wrapper.

Goal:
Compose a concise, actionable job-search digest. The deterministic wrapper sends it
to Telegram only after it finds an upcoming event, a Needs Action row, or an active
interview. Your final response IS the message, so output the brief text and nothing
else.

You are READ-ONLY. Do not write, create, or modify any file anywhere. Do not run
shell commands. Your entire job is: read the files below, compose the brief, output it.

Repo context:
The tracker repo is your current working directory (the runner launches you from the
repo root).

The runner has already resolved the active profile's work directory and substituted it
into this prompt as <WORK_DIR>. Do not resolve the profile yourself. If any path below
still contains an unsubstituted placeholder (the text "WORK_DIR" wrapped in angle
brackets), the runner failed to substitute it: output only a one-line error saying so.

Task:
1. Read <WORK_DIR>/jobs-tracker.md. It is a generated board. Read only:
   - `## Upcoming Events` and report every recorded event first.
   - `## Needs Action` and `## Interviewing`. Capture Company, Role, Outcome, and Last
     Contact for each row.

2. Compose the brief in this format. Be terse — this is a Telegram message, not a
   report:

   🎯 Job Search Action — YYYY-MM-DD

   📅 Upcoming:
   [each as: YYYY-MM-DD HH:mm timezone | Company — Role]

   🔴 Needs Action:
   [each as: Company — Role → Outcome (last contact YYYY-MM-DD)]

   💬 Active Interviews:
   [each as: Company — Role → Outcome (last contact YYYY-MM-DD)]

   Omit empty sections. Do not report health, pipeline totals, package queues, or
   routine activity. Watchdog handles operational failures separately.

3. Output the brief as your final response — no preamble, no commentary, no code
   fences. The wrapper pipes your response straight to Telegram.
